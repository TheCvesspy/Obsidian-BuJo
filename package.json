{
	"name": "obsidian-friday",
	"version": "2.1.0",
	"description": "Obsidian plugin: a personal ops console for knowledge workers — task aggregation, daily/weekly planning, sprints, JIRA dashboard, and team tracking.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "Quill of the Weavers",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.19.0",
		"obsidian": "latest",
		"typescript": "^5.3.0"
	}
}
